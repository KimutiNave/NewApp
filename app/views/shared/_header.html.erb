<nav class="navbar navbar-expand-lg bg-body-tertiary">
 <div class="container-fluid">
  <a class="navbar-brand">メニュー</a>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <li class="nav-item">
        <%= link_to 'トップページ', top_path, class: "nav-link active" %> <span class="sr-only">(current)</span>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          アカウント情報
        </a>
        <ul class="dropdown-menu">
          <li>
            <% if user_signed_in? %>
              <%= link_to 'アカウント情報変更', edit_user_registration_path(current_user.id), class: "dropdown-item", data: { turbo: false } %>
            <% end %>
          </li>
          <li><%= link_to '退会する', check_path(current_user.id), class: "dropdown-item", data: { turbo: false } %></li>
        </ul>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          メモの作成
        </a>
        <ul class="dropdown-menu">
          <li><%= link_to '保存用メモ作成', new_post_path, class: "dropdown-item", data: { turbo: false } %></li>
          <li><%= link_to 'エラー用メモ作成', new_another_post_path , class: "dropdown-item", data: { turbo: false } %></li>
        </ul>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          投稿一覧
        </a>
        <ul class="dropdown-menu">
          <li><%= link_to '保存用の投稿一覧', posts_path, class: "dropdown-item", data: { turbo: false } %></li>
          <li><%= link_to 'エラー用の投稿一覧', another_posts_path , class: "dropdown-item", data: { turbo: false } %></li>
        </ul>
      </li>
      <!-- li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          通知設定
        </a>
        <ul class="dropdown-menu">
          <% if user_signed_in? %>
            <li><%= link_to '通知作成', new_notification_setting_path, class: "dropdown-item", data: { turbo: false } %></li>
          <% end%>
        </ul>
      </li-->
      <li class="nav-item">
        <%= link_to 'サインアウト', destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
      </li>
    </ul>
    <nav class="navbar navbar-right">
      <div class="container-fluid">
        <li class="nav-item">
          <i class="fa-solid fa-bell fa-lg"></i>
        </li>
      </div>
    </nav>
  </div>
</nav>

